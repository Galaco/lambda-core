version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.12
        environment:
          GO111MODULE: "ON"
    working_directory: /go/src/github.com/galaco/lambda-core
    steps:
      - checkout
      - run: go get -v -t -d ./...
      - run: go test -v ./...
